# SAI-frontend-prototype

This is a code bundle for SAI-frontend-prototype. The original project is available at https://www.figma.com/design/nKVDmJWNHJsMwzoJwCc6ps/SAI-frontend-prototype.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
